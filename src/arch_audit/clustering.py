"""Clustering stubs."""

import numpy as np


def cluster_files(distance_matrix: np.ndarray, distance_threshold: float = 0.7) -> list[list[int]]:
    """Cluster file indexes using agglomerative clustering (placeholder)."""
    _ = (distance_matrix, distance_threshold)
    return []
