"""Co-change matrix stubs."""

import numpy as np


def build_cochange_matrix(commits: list[list[str]]) -> tuple[list[str], np.ndarray]:
    """Build file index and co-change matrix (placeholder)."""
    _ = commits
    return [], np.zeros((0, 0), dtype=float)
