"""Git history extraction stubs."""

from pathlib import Path


def extract_commits(repo_path: Path) -> list[list[str]]:
    """Return a parsed list of commits (placeholder)."""
    _ = repo_path
    return []
